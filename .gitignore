# Local archive snapshots — never publish to the live repo
$Archive/
**/$Archive/

# OS / editor cruft
desktop.ini
Thumbs.db
.DS_Store
*.tmp
~$*

# GoodSync sync metadata & PULSE logs — local only, never publish
_gsdata_/
